## Summary

<!-- What does this PR do? Why? One paragraph. -->

## Changes

<!-- Bullet list of what changed and in which files. -->

-

## Testing

- [ ] All existing tests pass (`pytest -q`)
- [ ] New tests added for new behaviour
- [ ] Coverage remains at 100% (`pytest --cov-fail-under=80`)
- [ ] Lint passes (`ruff check backend ingestion tests`)
- [ ] Type-check passes (`mypy backend ingestion router db`)
- [ ] Security scan passes (`bandit -c pyproject.toml -r backend ingestion router db`)
- [ ] Frontend tests pass (`cd frontend && npm run test`)

## Related issues

Closes #
